import 'package:flutter/material.dart';
import 'package:get/get.dart';

class AudioPlayerMiddleWare extends GetMiddleware {
  @override
  RouteSettings? redirect(String? route) {
    //to do for future
    return super.redirect(route);
  }
}